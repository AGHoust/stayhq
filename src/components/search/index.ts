// Main search components
export { default as SearchBarPrimary } from './SearchBarPrimary';
export { default as SearchBarSlim } from './SearchBarSlim';

// Re-export search hooks and types
export { useSearch, type SearchState } from '../../hooks/useSearch';
